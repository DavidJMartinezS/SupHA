% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st_trans_sup_ha.R
\name{setup_arcpy_env}
\alias{setup_arcpy_env}
\title{Configura el entorno de Python para usar arcpy}
\usage{
setup_arcpy_env(python_path = NULL)
}
\arguments{
\item{python_path}{(Opcional) Una cadena de texto con la ruta completa al
ejecutable de Python de ArcGIS Pro (p. ej., \code{python.exe}). Si es \code{NULL},
se intentará la detección automática.}
}
\value{
Imprime mensajes de estado en la consola. Devuelve \code{TRUE} (invisiblemente)
si la configuración fue exitosa, y lanza un error si falla.
}
\description{
Esta función de ayuda busca y configura el entorno de Python que incluye
el paquete \code{arcpy} de ArcGIS Pro. Facilita la inicialización de \code{reticulate}
para que las funciones que dependen de \code{arcpy} funcionen correctamente.
}
\details{
La función opera de la siguiente manera:
\enumerate{
\item Si se proporciona un \code{python_path}, intentará usar esa ruta directamente.
\item Si \code{python_path} es \code{NULL} (por defecto), la función intentará detectar
automáticamente la instalación de ArcGIS Pro en sistemas Windows
buscando en el registro del sistema.
\item Si la detección automática falla, mostrará un mensaje de error con
instrucciones para que el usuario proporcione la ruta manualmente.
}

Esta función solo necesita ejecutarse una vez por sesión de R, antes de
llamar a cualquier función que dependa de \code{arcpy}.
}
\examples{
\dontrun{
  # Opción 1: Detección automática (recomendado en Windows)
  setup_arcpy_env()

  # Opción 2: Especificar la ruta manualmente
  setup_arcpy_env(
    python_path = "C:/Program Files/ArcGIS/Pro/bin/Python/envs/arcgispro-py3/python.exe"
  )
}
}
